#include <string>

namespace Operation{
    class MatrixOperation{
    public:
        MatrixOperation(std::string operation, std::string inputFiles, std::string outputFile, CacheManager cacheManager);
    };
























}